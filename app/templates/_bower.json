{
  "name": "<%= _.slugify(projectName) %>",
  "version": "0.0.0",
  "dependencies": {
    <% if (includeNormalize) { %>"normalize-scss": "^3.x.x",<% } %>
	<% if (includeScut) { %>"scut": "^1.0.1",<% } %>
	<% if (includeBreakpoint) { %>"breakpoint-sass": "^2.4.0",<% } %>
    "modernizr": "~2.6.2",
    "jquery": "~1.9.1"
  },
  "devDependencies": {}
}
